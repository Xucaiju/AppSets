<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="xcj.appsets">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.REQUEST_DELETE_PACKAGES" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:name=".AppSetsApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".AlipayDemoActivity"></activity>
        <activity android:name=".DownloadManagerActivity" />
        <activity android:name=".ui.RecoverPasswordActivity" />
        <activity android:name=".ui.DownloadActivity" />
        <activity android:name=".ui.UserProfileActivity" />
        <activity android:name=".TestChartActivity" />
        <activity android:name=".ui.TimeLineActivity" />
        <activity android:name=".ui.SearchResultActivity" />
        <activity android:name=".ui.TodayDetailsActivity" />
        <activity
            android:name=".ui.SettingsActivity"
            android:label="@string/title_activity_settings"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.AboutActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.SignupActivity"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize|stateHidden" />
        <activity android:name=".ui.CategoryActivity" />
        <activity android:name=".ui.AppDetailsActivity" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <activity
            android:name=".ui.LoginActivity"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme.LaunchScreenTheme"
            android:windowSoftInputMode="adjustResize|stateHidden">
            <intent-filter>
                <action android:name="loginTest" />
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.MainActivity"
            android:launchMode="singleTask">

            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>
        <activity
            android:name=".ui.SearchActivity"
            android:windowSoftInputMode="adjustPan|stateHidden" />

        <service android:name=".service.ApiValidateService" />
        <service android:name=".service.AppSetsFirebaseSerivce" />
    </application>

</manifest>